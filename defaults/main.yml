---
# Default webmin variables

#Webmin ports to listen on
webmin_listen: "10000"
webmin_port: "10000"

# Set SSL enabled?  Webmin use 1 for true and 0 for false in config
webmin_ssl: "1"

#If behind reverse proxy, add referer URL here
webmin_reverse_proxy_referer: ''
